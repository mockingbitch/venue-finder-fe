# API Base URL (used by the frontend to fetch venues and auth)
NEXT_PUBLIC_API_URL=http://localhost:8089/api

# Set to "false" to use real API. Default is mock (no backend required).
NEXT_PUBLIC_USE_MOCK_DATA=false
